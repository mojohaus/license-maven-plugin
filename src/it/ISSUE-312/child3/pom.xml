<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.codehaus.mojo.license.test.ISSUE-312</groupId>
    <artifactId>parent</artifactId>
    <version>1.0</version>
  </parent>
  <artifactId>child3</artifactId>
  <dependencies>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>child1</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>child1</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
    </dependency>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>child2</artifactId>
      <version>${project.version}</version>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>license-maven-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>download-licenses</goal>
            </goals>
            <phase>generate-resources</phase>
            <configuration>
              <organizeLicensesByDependencies>true</organizeLicensesByDependencies>
              <errorRemedy>xmlOutput</errorRemedy>
              <licenseUrlReplacements>
                <!-- Apache 2 -->
                <licenseUrlReplacement>
                  <regexp>https?://www.apache.org/licenses/LICENSE-2.0.txt</regexp>
                  <replacement>${project.baseUri}/src/licenses/local/apache20.txt</replacement>
                </licenseUrlReplacement>
              </licenseUrlReplacements>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
